<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.happyhouse.model.mapper.InterestAptMapper">

	<select id="getInterestApt" parameterType="String" resultType="HouseDto">
		select * from interest_addr
		where userid=#{userid}
	</select>
</mapper>